
#include <stdio.h>

float find_largest (int a, int b,int c)
{
  float;
  if ( a > b)
    if (a > c)

      Else  if (b > a)
  return c;
}

int main()
{
  float x,y,z;
  printf("Enter the three numbers:\n");
  scanf("%f%f%f", &a, &b, &c);
  float = find_largest(a,b,c);
  printf("The largest number among %f and  %f is %f\n",a,b,c,);
  return 0;
}